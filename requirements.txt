# ── Core web framework ────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
httpx>=0.25.0
websockets>=11.0.0

# ── TTS / STT ─────────────────────────────────────────────────────────────────
edge-tts>=6.1.9
faster-whisper>=1.0.0

# ── Memory / Vector DB ────────────────────────────────────────────────────────
chromadb>=0.4.0

# ── NOTE: numpy compatibility ─────────────────────────────────────────────────
# Some transitive deps (tts==0.22.0, gruut) declare numpy<2.0 but numpy 2.x
# works fine in practice for SARA's actual usage (edge-tts + faster-whisper).
# If you hit numpy-related runtime errors, pin: numpy>=1.24.0,<2.0.0